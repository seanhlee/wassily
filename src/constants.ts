/** Shared layout and typography constants. */

/** Side length of a swatch square and height of a ramp stop (px). */
export const SWATCH_SIZE = 48;

/** Base font used everywhere in the app. */
export const FONT =
  '"PP Neue Montreal", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';

/** Base UI font size (px). */
export const FONT_SIZE = 12;

/** Interface text weight: PP Neue Montreal Regular. */
export const FONT_WEIGHT_UI = 400;

/** Authored canvas text weight: PP Neue Montreal Regular. */
export const FONT_WEIGHT_TEXT = 400;

/** Available ramp stop count presets. */
export const RAMP_STOP_PRESETS = [3, 5, 7, 9, 11, 13] as const;
